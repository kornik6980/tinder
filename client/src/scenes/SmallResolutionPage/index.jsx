import React from "react";

const SmallResolutionPage = () => {
	return (
		<div style={{ color: "#fff", padding: "20px", textTransform: "uppercase" }}>
			resolution not supported
		</div>
	);
};

export default SmallResolutionPage;
